// src/jobs/exampleJob.js
const exampleJob = () => {
    console.log(`[${new Date().toISOString()}] Example Job đang chạy...`);
    // Thực hiện logic cần thiết tại đây
};

module.exports = { exampleJob };
